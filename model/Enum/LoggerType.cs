using System;
using System.Collections.Generic;
using System.Text;

namespace ShoppingCart.Domain.Model.Enum
{
    public enum LoggerType
    {
        DatabaseLogger = 1,
        FileLogger = 2,
        EmailLogger = 3
    }
}
